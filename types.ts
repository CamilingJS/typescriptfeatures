//Type annotations: Code we add to tell Typescript what type of value a variable will refer to
//Type inference: Typescript tries to figure out what type of vlaues a variable refers to

const today = new Date(); 
today.getMonth();


const person = {
  age: 20
}

class Color {

}

const red = new Color();


